# CUDA build: uses GPU when drivers are present, falls back to CPU otherwise.
# Driver 580 / CUDA 13.0 → cu130. Older drivers: cu126. See pytorch.org/get-started/locally/
--extra-index-url https://download.pytorch.org/whl/cu130
numpy>=1.26
torch>=2.2
transformers>=4.40
